<!doctype html>
<html {{#if section }}data-current="{{ section }}"{{/if}} lang="en">

{{> components/head }}

<body>

<header>
  <em>
    <a href="{{ url '/' }}">{{ metadata.title }}</a>
  </em>
  {{> components/nav }}
</header>

<main>
  {{{ layoutContent }}}
</main>

{{> components/footer }}

</body>

</html>
